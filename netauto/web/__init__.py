"""Web GUI for netauto.

A small self-hosted server so a team can use the toolkit from a browser. It
runs inside the network it manages, holds the device credentials in its own
environment, and exposes only the read paths.
"""
